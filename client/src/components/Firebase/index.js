import FirebaseContext, { withFirebase } from './context';
import Firebase from './firebase'; // Firebase import path

export default Firebase; // Export Firebase as default
export { FirebaseContext, withFirebase };